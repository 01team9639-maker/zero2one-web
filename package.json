{
  "name": "zero2one-web",
  "private": true,
  "description": "Tooling for the ZERO 2 ONE site. The site itself is static and needs none of this at runtime.",
  "scripts": {
    "build": "sh tools/build.sh",
    "visual:baseline": "node tools/visual_check.js --baseline",
    "visual:check": "node tools/visual_check.js"
  },
  "devDependencies": {
    "esbuild": "0.28.1",
    "playwright": "1.62.1",
    "sharp": "0.35.3"
  }
}
